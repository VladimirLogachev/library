type Query {
  authors: [Author!]!
  books: [Book!]!
}

type Mutation {
  createAuthor(authorName: String!): Boolean!
}

type Author {
  name: String!
  books: [Book!]!
}

type Book {
  title: String!
  coverImageUrl: String!
  author: Author!
}
