type Query {
  authors: [Author!]!
  books: [Book!]!
}

type Mutation {
  createAuthor(author: AuthorInput!): Boolean!
}

type Author {
  name: String!
  books: [Book!]!
}

input AuthorInput {
  name: String!
}

type Book {
  title: String!
  coverImageUrl: String!
  author: Author!
}
